apiVersion: grafana.integreatly.org/v1beta1
kind: Grafana
metadata:
  name: grafana
  namespace: grafana
  labels:
    dashboards: "grafana"
spec:
  external:
    url: http://grafana.grafana:80
    adminPassword:
      name: grafana
      key: admin-password
    adminUser:
      name: grafana
      key: admin-user
